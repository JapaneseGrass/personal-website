# Drink Machine media

Drop photos and short videos here, then reference them from
`src/content/projects/drink-machine.md` under `media:`.

Supported: images (.jpg/.png/.webp) and video (.mp4/.webm).
Tip: keep videos short and compressed (a few MB) so the page loads fast.
